## What this project does

This is a simple goal-based AI agent.
It is designed to run using a single command.

## Intended usage (later)

The agent will be executed like this:

python agent/main.py "Generate YouTube Short ideas on psychology"

The agent will automatically process the goal and return results.
